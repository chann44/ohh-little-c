#include <stdio.h>
#include <stdbool.h>

int main(int argc, char const *argv[])
{
    int a; // integer  it can stor negative and positive both 
    unsigned int b; // it can only store positive numbers
    char c; // Character TYpe 
    short d; // short int 
    long e; // long it 
    float f; // floating point values 0.2 
    bool h; // Boolean values like TRUE or FALSE
    /* code */
    //  initialization of variables 
    a = 0;
    char greeting_a[5] = {'h', 'e', 'l'};
    // pointer variable that stores the mem address of "hello"
    char* greeting_c = "Hello";
    return 0;
}
